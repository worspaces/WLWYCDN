name: Combine Parts

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  combine:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify parts and combine
        run: |
          set -e
          mkdir -p "Games/snowball"
 
          if [[ -f "Games/snowball/Builds.wasm.br.000.part" && -f "Games/snowball/Builds.wasm.br.001.part" && -f "Games/snowball/Builds.wasm.br.002.part" ]]; then
            echo "All pieces found. Combining..."
            cat "Games/snowball/Builds.wasm.br.000.part" "Games/snowball/Builds.wasm.br.001.part" "Games/snowball/Builds.wasm.br.002.part" > "Games/snowball/Builds.wasm.br"
            echo "Combined -> Games/snowball/Builds.wasm.br"
            ls -lh "Games/snowball/Builds.wasm.br"
          else
            echo "ERROR: Missing one or more pieces."
            exit 1
          fi

      - name: Commit and push combined file
        run: |
          set -e
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git rm -f -- "Games/snowball/Builds.wasm.br.000.part" "Games/snowball/Builds.wasm.br.001.part" "Games/snowball/Builds.wasm.br.002.part" || true
          git add "Games/snowball/Builds.wasm.br"
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Combine parts into Builds.wasm.br and remove pieces"
            git push
          fi
