name: Combine Parts

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  combine:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify parts and combine
        run: |
          set -e
          mkdir -p "Games/test"
 
          if [[ -f "Games/test/BuildeWeb.data.br.000.part" && -f "Games/test/BuildeWeb.data.br.001.part" ]]; then
            echo "All pieces found. Combining..."
            cat "Games/test/BuildeWeb.data.br.000.part" "Games/test/BuildeWeb.data.br.001.part" > "Games/test/BuildeWeb.data.br"
            echo "Combined -> Games/test/BuildeWeb.data.br"
            ls -lh "Games/test/BuildeWeb.data.br"
          else
            echo "ERROR: Missing one or more pieces."
            exit 1
          fi

      - name: Commit and push combined file
        run: |
          set -e
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git rm -f -- "Games/test/BuildeWeb.data.br.000.part" "Games/test/BuildeWeb.data.br.001.part" || true
          git add "Games/test/BuildeWeb.data.br"
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Combine parts into BuildeWeb.data.br and remove pieces"
            git push
          fi
