name: Combine Parts

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  combine:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify parts and combine
        run: |
          set -e
          mkdir -p "Games/pokemon-adventure-rescue-team"
 
          if [[ -f "Games/pokemon-adventure-rescue-team/pokemon_mystery_dungeon.gba.000.part" && -f "Games/pokemon-adventure-rescue-team/pokemon_mystery_dungeon.gba.001.part" ]]; then
            echo "All pieces found. Combining..."
            cat "Games/pokemon-adventure-rescue-team/pokemon_mystery_dungeon.gba.000.part" "Games/pokemon-adventure-rescue-team/pokemon_mystery_dungeon.gba.001.part" > "Games/pokemon-adventure-rescue-team/pokemon_mystery_dungeon.gba"
            echo "Combined -> Games/pokemon-adventure-rescue-team/pokemon_mystery_dungeon.gba"
            ls -lh "Games/pokemon-adventure-rescue-team/pokemon_mystery_dungeon.gba"
          else
            echo "ERROR: Missing one or more pieces."
            exit 1
          fi

      - name: Commit and push combined file
        run: |
          set -e
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git rm -f -- "Games/pokemon-adventure-rescue-team/pokemon_mystery_dungeon.gba.000.part" "Games/pokemon-adventure-rescue-team/pokemon_mystery_dungeon.gba.001.part" || true
          git add "Games/pokemon-adventure-rescue-team/pokemon_mystery_dungeon.gba"
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Combine parts into pokemon_mystery_dungeon.gba and remove pieces"
            git push
          fi
